#!/bin/sh

# pre-commit hook for dotfiles repo that is copied into the .git/hooks directory
dotfiles_dir="$HOME"/.dotfiles

# run brew bundle dump and stage file to keep Brewfile always up to date
if [ "$PWD" = "$dotfiles_dir" ]; then
    brew bundle dump --force --file=$dotfiles_dir/Brewfile
    git add Brewfile
fi